[project]
name = "resolve-timer"
version = "0.1.0"
description = "DaVinci Resolve Studio race-run timing overlay tool"
requires-python = ">=3.10"
dependencies = [
  "PyYAML>=6.0",
]

[project.scripts]
resolve-timer = "resolve_timer.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
